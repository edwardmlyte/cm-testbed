apiVersion: v1
kind: Service
metadata:
  name: {{ include "d.fullname" . }}
  labels:
    {{- include "d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "d.selectorLabels" . | nindent 4 }}
